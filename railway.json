{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "apt update && apt install -y ffmpeg aria2"
  },
  "deploy": {
    "startCommand": "uvicorn Api:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
